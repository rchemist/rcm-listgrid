# EntityForm API 구현 wave 브리프 (실행급 — opus/sonnet 세션 실행 계약)

> **지위**: [공개 API 스펙](./entityform-public-api-spec.md)(규범)의 **실행 계약**. 이 문서의 태스크는 하위 티어 세션이 재유도 없이 실행 가능해야 한다(harness team-conventions §설계 산출물 = 실행급 계약). **스펙 §번호를 인용할 수 없는 판단이 나오면 구현 금지** — §Open Questions로 올리고 스펙 개정이 먼저다(스펙 §10 게이트 4).
> **작성**: 2026-07-11 (fable). W1~W4 = 태스크 단위 완전 명세. W5~W7 = 계약 수준 + **wave-entry 브리핑 pass 필수**(아래 §W5 규칙).
> **파일 경로 주의**: 인용 file:line은 2026-07-11 HEAD 기준 — 착수 시 재확인(선행 wave가 옮겼을 수 있음).

## 전역 규칙 (모든 wave·태스크 공통)

- **게이트**: 태스크마다 `npm run type-check && npm run typecheck:packages && npm test && npm run lint && npm run format:check && npm run build`; wave 종료 시 +E2E 16+ 및 **CAP 대조**(이 wave가 선언한 CAP-ID 전부 소화 or §Progress notes에 명시 이월).
- **hot-file 직렬화**: `packages/schema-core/src/entity-form.ts` · `packages/state/src/form-store.ts` · `packages/react/src/components/ViewEntityForm.tsx`를 건드리는 태스크는 **같은 wave 안에서 순차**(병렬 fan-out 금지).
- **전역 Do-NOT**(PROGRESS Handoff 승계): ① 훅/빌더가 store 직접 수신 금지(FormMutator 경유) ② 동적 mutation 후 `entityForm.getFields()` 직접 읽기 금지(store.fieldDefs) ③ exactOptionalPropertyTypes 조건 spread 트랩 — 옵션 프로퍼티는 `p?: T | undefined`로 선언하고 조건 spread 대신 명시 대입 ④ SOUND 내부(store/EF1-7/seam) 재작성 금지 — 공개 표면 재설계임 ⑤ 검증 없는 ✅ 금지(증거 1줄).
- **테스트 정책**: 기존 테스트의 *이름 변경 동반 수정*은 허용(rename wave 특성), **행동 약화 금지** — 기대값을 바꾸는 수정은 스펙 §를 인용해야 한다.

## W1 — 표면 정비 (rename·큐레이션·계수) · CAP-12 일부

| id | 태스크 | 파일 | before → after | 증명 | Do-NOT |
|---|---|---|---|---|---|
| W1-1 | `readonly`→`readOnly` 전면 개명 (스펙 L2) | `packages/schema-core/src/field/{form-field.ts:49, field-meta.ts:13, entity-field.ts}` + 전 필드 클래스 + `packages/state/src/form-store.ts`(meta 접근) + react 소비처 + 테스트 | `readonly?: ReadOnlyType` → `readOnly?: ReadOnlyType` · `FieldMetaOverride.readonly` → `.readOnly` · `isReadonly()` → `isReadOnly()` | full gate — 잔존 `\breadonly\??:` grep 0 (TS 키워드 `readonly` 수식어는 제외) | 행동 변경 0 — 식별자 개명만. `withReadOnly` 빌더는 이미 대문자(무변경) |
| W1-2 | `placeHolder`→`placeholder` 개명 (L2) | `form-field.ts`(prop+`withPlaceHolder`), 렌더러들, ui-default `TextInputProps` 인접 | `placeHolder?` → `placeholder?` · `withPlaceHolder` → `withPlaceholder` | full gate — `placeHolder` grep 0 | — |
| W1-3 | EntityForm 정체성 정리 (스펙 §3.1) | `packages/schema-core/src/entity-form.ts:93-95,129,265-268` + 호출처(`packages/state/src/initialize-form-store.ts`, apps/sample) | ctor `(name, fetchUrl)` → `(name, url)`(trailing slash 정규화 저장) · `getName()/getUrl()` 삭제 · `readonly name/url` public 유지 | full gate — `getName\(\)\|getUrl\(\)` 리포 grep 0 | `getFetchUrl` 부활 금지(스펙 §3.6) |
| W1-4 | addFields 입력 개명 (§3.2) | `entity-form.ts:82-88` AddFieldsInput + 전 사용처(sample entities 다수) | `fieldGroup?:` → `group?:` (TabInput/GroupInput에 `requiredPermissions?: string[] \| undefined` 슬롯 추가 — 소비는 W3-1) | full gate | requiredPermissions **소비 로직은 W3-1** — 여기선 타입만 |
| W1-5 | `withoutField`/`withoutTab`/`withTab`/`withGroup` 신설 + `EntityForm.setTabHidden` 제거 (§3.2, L1) | `entity-form.ts`(setTabHidden :227 제거) + 테스트 | 신설 4 멤버 — 시그니처는 스펙 §3.2 표 그대로. setTabHidden 사용처는 `withTab(id,{hidden})`으로 | 신규 unit: withoutTab이 getTabs()에서 구조 제거·withTab({hidden})은 유지+숨김 구분 | mutator/store의 `setTabHidden`(런타임)은 **유지** — 제거는 EntityForm 것만 |
| W1-6 | 배럴 큐레이션 (§7) | `packages/schema-core/src/index.ts:226`(SCHEMA_CORE_VERSION), `permission.ts:58`(PermissionPolicy), `index.ts:94`(isEquals/isEqualCollection) 제거 · `packages/react/src/index.ts`에 useFieldMeta(`providers/form-store.tsx:58`)/useReferenceResolver(`providers/adapter.tsx:81`) 추가 · `apps/sample/app/page.tsx:13` 수정 | export 4건 -, 2건 + | full gate + 계수 스크립트(W1-7) 통과 | isEquals 등 **내부 사용은 유지**(배럴만 제거) |
| W1-7 | 공개 표면 계수 스크립트+CI (§10 게이트 2) | `scripts/count-public-surface.mjs`(신설) + CI 워크플로 | atomic member 규칙(§10) 구현 — EntityForm ≤45·루트 ≤120·`/schema` ≤180 초과 시 fail | CI green + 현재값 기록 | 계수 규칙 임의 완화 금지 |

**W1 종료 게이트**: full gate + E2E 16 + `grep -rn "getName()\|placeHolder\|SCHEMA_CORE_VERSION"` 0 + 계수 리포트 커밋.

## W2 — 훅 통합 + FormRuntime/FormController · CAP-04·07(주입점)·11·14·21·25·26

> hot-file 3종 전부 관통 — **전 태스크 순차**, fan-out 금지.

| id | 태스크 | 파일 | before → after | 증명 | Do-NOT |
|---|---|---|---|---|---|
| W2-1 | 훅 개명+`InitContext` 도입+EF7 값세터 이전 | `entity-form.ts:149-166,187-235`(withOnChanges/OnFetchData/OnInitialize→onChange/onInit 통합, setValue/setFetchedValue 제거) · `packages/state/src/initialize-form-store.ts:105-182`(step 4~5를 InitContext 기반으로: `{form, data?, values{get/set/setFetched}, setMeta, session?, renderType}`) · `packages/schema-core/src/__tests__/entity-form-value.test.ts` 재작성 | 스펙 §4.1 InitContext 그대로. onFetchData+onInitialize → `onInit` 1종(ctx.data 분기). **ctx.setMeta는 store 초기 meta seed로 축적**(createFormStore options에 `initialMeta?` 추가) | 기존 EF3/EF7 테스트의 행동 등가 재작성(초기화 순서·override 우선순위 hook.set > fetched > default 보존) + 신규: onInit에서 setMeta({hidden})→store.getMeta 반영 | **BIND→hooks→REBIND→build 순서 불변**(EF7 회귀 재현 금지). onInit는 save 후 재발화하지 않음 |
| W2-2 | FormMutator additive 확장 (§6.1) | `packages/schema-core/src/field/form-mutator.ts` + `packages/state/src/form-store.ts`(구현) | +`getRenderType(): RenderType` +`getSession(): Session \| undefined` | unit: onChange 핸들러에서 renderType 분기 | 기존 7멤버 시그니처 무변경 |
| W2-3 | `messages` 채널 (§6.1) | `form-store.ts:82,142`(formErrors→messages+3액션) · `ViewEntityForm.tsx:117,210-216`(배너 렌더) | `formErrors: string[]` → `messages: FormMessage[]` — 스펙 §6.1 형 그대로 | unit: add/remove/clear({includePersistent}) round-trip + 배너 렌더 테스트 | messages를 **필드 에러 저장소로 쓰지 않음**(필드 에러는 slice.errors) |
| W2-4 | `serializeValue` seam (§5.2) | `form-field.ts`(기본 구현 `{[name]: value}`) · `many-to-one-field.ts:55 인접`(override `{[idField]: id}`) · `form-store.ts:565-600` toSaveData 재작성(duck-cast :583 제거) | 스펙 §5.2 — **항상 keyed 맵 반환**, toSaveData는 병합+dotted 중첩만 | 기존 toSaveData 특성화 전량 green(M2O 평탄화·exceptOnSave·권한 제외 CAP-01 불변) + 신규: 커스텀 필드 serializeValue override | InlineMap/Datetime 등 객체값 필드의 자기-이름 중첩이 M2O 분기와 충돌하지 않음을 테스트로 고정 |
| W2-5 | `FormRuntime`(schema)+`createFormController`(state)+save/delete 훅 축+EF6 대체 | 신설 `packages/schema-core/src/form-runtime.ts`(FormRuntime/SaveOutcome/DeleteOutcome — adapter.ts의 BackendError 참조) · 신설 `packages/state/src/form-controller.ts` · `entity-form.ts`(onBeforeSave/onAfterSave/onBeforeDelete/onAfterDelete 4훅+**withSubmitTransform :168,297 제거**) · `packages/schema-core/src/__tests__/submit-transform.test.ts`(7건)+`packages/state/src/__tests__/store.test.ts:108,122` onBeforeSave로 재작성 · `apps/sample/lib/entities/collabo.ts:374`+`app/collabo/new/page.tsx` 이관 | 스펙 §6.2 플로우 **그대로**(순서: capability→validate→toSaveData→onBeforeSave→revision→adapter→에러매핑(name-키·suppress-generic)/성공(clear-on-success)→onAfterSave) | controller unit(성공/검증실패/cancel/서버 fieldErrors 매핑/suppress-generic/clear-on-success 각 1) + collabo E2E 유지 | schema가 /state를 import하지 않음(**FormRuntime은 schema 소유**, 검증 feasibility-1). onSave 전체 오버라이드 부활 금지 |
| W2-6 | list 훅 축 (§4) | `entity-form.ts`(onBeforeListFetch/onAfterListFetch) · `packages/state/src/list-store.ts:24,42-`(옵션 `entityForm?` + fetch 파이프에 훅 dispatch + `setSearchForm` 경로) | 스펙 §4.1 BeforeListFetchContext(**setSearchForm이 실주입 경로** — 검증 feasibility-4) | unit: onBeforeListFetch의 setSearchForm(filter 추가)가 adapter.list 도달 body에 반영 · onAfterListFetch setRows 반영 | SearchForm 불변성 유지(변형은 새 인스턴스) |
| W2-7 | `useEntityForm` (react, §7) | 신설 `packages/react/src/hooks/use-entity-form.ts`(기존 use-entity-form-initializer 계승+controller 동봉) + barrel + sample 페이지 전환 | `{store, entityForm, controller, loading, error}` 반환 | 렌더 테스트(cancellation-safe 계약 유지) + sample college 페이지가 controller.save 사용 | 기존 initializer 훅은 @deprecated 유지 1-wave 후 제거 |
| W2-8 | changeSelectOptions 배열-clause 레이스 fix (§Needs Review 처분 #27 전환) | `packages/schema-core/src/onchanges/change-select-options.ts` + 테스트 | 동일 필드를 겨냥한 2 clause에서 **unmatched revert가 matched apply를 clobber하지 못하게** — clause 평가를 2-pass(match 판정 후 일괄 적용, matched 필드는 revert 대상 제외)로 재작성 | unit: clause 순서 뒤집어도 settled options 동일(레이스 재현 테스트 red→green) | 빌더 공개 시그니처 무변경 |

**W2 종료 게이트**: full gate+E2E + CAP-04/11/14/21/25/26 대조 + "구 결함 원장 §1~9 중 이 wave가 봉인한 항목"(값세터 파편화·이중발화·view-실행 훅·inert formErrors) 각 1줄 증거.

## W3 — 권한·능력·액션 · CAP-02·03·06·08·09·22·27

| id | 태스크 | 핵심 파일 | 계약(스펙 §) | 증명 |
|---|---|---|---|---|
| W3-1 | 탭/그룹 requiredPermissions 소비+가시성 파생 | entity-form.ts(TabInput/GroupInput→TabDef/FieldGroupDef 보존) · ViewEntityForm(deriveTabs: isPermitted+hasVisibleContent — sync 근사는 blueprint 사전판정 GO) | §3.2, CAP-02·03 | unit: 무권한 탭/그룹/빈그룹 숨김 + E2E 1 |
| W3-2 | withCapabilities/getCapabilities+해석 | entity-form.ts · form-controller.ts(save/delete 게이트) · ViewEntityForm(빌트인 어포던스) | §3.4, CAP-06·22 | unit: 조건부(delete:(ctx)=>role) create/update/delete 각 게이트 |
| W3-3 | addAction/getActions+FormAction(render/className)+액션 바+slots | entity-form.ts · ViewEntityForm(빌트인 Save/Delete 파생+replaces 병합+visible/enabled 해석+render 슬롯) · slots {title,header,actions} | §3.4·§7, CAP-09 | unit: replaces:'save' 교체·visible 조건 숨김·render 커스텀 노드 + E2E 커스텀 버튼 1 |
| W3-4 | delete flow E2E | form-controller(이미 W2-5)+ViewEntityForm Delete 버튼+confirm(messages registry) | §6.2, CAP-08 | **E2E: College create→delete→목록에서 소멸** |
| W3-5 | withReadOnly+formReadOnly | entity-form.ts(§3.1) · form-store(seed) · FieldRenderer(effective OR) · M2O 자식 전파 | §3.1·§6.1, CAP-27 | unit: 전 필드 readOnly+Save 숨김 · **M2O 임베드된 child readOnly 폼 유지 테스트**(gjcu 패턴) — save 하드게이트 없음을 명시 테스트 |

## W4 — 폼 완결 · CAP-05·07·10·13·23

| id | 태스크 | 계약(스펙 §) | 증명 |
|---|---|---|---|
| W4-1 | withTitle/getTitle(항상 해석)+slots.title | §3.1 해석 체인 text→fromField→name필드→id→기본문구 | unit: 무인자 호출이 항상 비어있지 않은 문자열(구 `''` 버그 봉인) |
| W4-2 | withSteps 위저드+Stepper UI(create 모드) | §3.2 StepDef · clone 무손실(hidden step 보존) | unit: clone 후 getSteps 동일 + E2E 3-step 생성 흐름 1 |
| W4-3 | AsyncValidation+asyncState+버튼 어포던스 | §5.3 | unit: trigger 'change'(debounce)/'button' tri-state + E2E 중복확인 1 |
| W4-4 | withRevision 주입 | §3.1 — save payload+`adapter.remove(url, ids, revision?)` 시그니처 확장 | unit: 설정 시에만 주입·미설정 undefined(구 always-truthy 봉인) |
| W4-5 | withMeta/getMeta (merge) | §3.1 — shallow-merge·undefined 키 제거 | unit: 프리셋 2회 호출 합성(클로버 없음 — 검증 dx-6 봉인) |

## W5~W7 — 계약 수준 (wave-entry 브리핑 pass 필수)

**규칙**: W5~W7은 규모(구 list 생태계 ~10.3k LOC)와 선행 의존 때문에 지금 태스크 분해하면 스펙-드리프트가 생긴다. 각 wave 착수 시 **entry 브리핑 pass**(스펙 §5.1·§7·§2·§3.5 + [8그룹 map](../analysis/2026-07-11/eg-group-capability-maps.md)의 해당 그룹 인용)로 이 문서에 W3/W4급 태스크 표를 **먼저 추가·커밋**한 뒤 실행한다. 스펙에 없는 판단이 필요하면 스펙 개정이 선행(§10 게이트 4). **계수 임계값 재검증(필수)**: entry pass는 그 wave의 신규 `/schema`·EntityForm·root 심볼을 스펙 §10-A 인벤토리 표에 추가하고, 합계가 임계값(EntityForm 55·root 120·`/schema` 190)에 근접/초과하면 같은 entry 커밋에서 표 근거와 함께 임계값을 재산정한다(2026-07-11 W4 착수 전 1차 재산정 완료 — §10-A).

- **W6 data-transfer** (CAP-16·17): withDataTransfer 표면(schema) → `/excel` subpath(DataExporter/Importer, xlsx optional) → ViewListGrid 툴바 opt-in. :448 대칭 코드 공유로 구조적 fix. 참조 map: `DATA TRANSFER/EXCEL`.
- **W7 패키징+마이그레이션** (CAP-24·25): `@rchemist/listgrid` subpath exports 맵·peers 재선언·headless fixture(React 0 빌드 테스트)·MIGRATION.md 0.3→0.4 전수표(스펙 §9 확장)+codemod 스크립트·adapter headers 함수형.

## W5 — list-track (entry-brief pass ✅ 2026-07-12 · CAP-18·19·20)

> **entry 근거**: 스펙 §5.1(withList/withFilter 신설)·§7(ViewListGrid/레지스트리)·§3.6(순수 질의 내부화)·§8 CAP-18/19/20·§9(마이그레이션)·§10-A(계수). 현 코드 실태 서베이(2026-07-12 sonnet) — file:line은 HEAD `9209f8f` 기준(착수 시 재확인). **공개 표면은 작다**(필드 빌더 2·타입 2·레지스트리 2); 구현 UI 인프라가 phase급(8그룹 map ~10.3k LOC 경고). 스펙이 순수 get* 질의(getListFields/getFilterableFields/getListableFieldOrder/getViewOrder/getAdvancedSearchFields)를 **소비자 사용 0 → 엔진 내부 파생**으로, onFetchListData를 **onAfterListFetch(W2-6 기출하)로** 이미 흡수 → W5 = 필드 선언 + react 파생/레지스트리 + 고급검색으로 축소.

**wave-entry 결정(스펙 인용 or 비크리티컬 모델 판정 — §10 게이트 4 준수)**:
1. **페이지 셸 = 문서 가이드(컴포넌트 아님)** [§12 Open 해소]: 스펙 §7 react 표에 페이지-셸 컴포넌트 없음 + §9가 "호스트 페이지 셸·MIGRATION 전용 절"로 프레이밍 + C7(호스트가 페이지 chrome 소유). apps/sample 6개 리스트 페이지가 이미 bare 컴포지션(main→헤더행+"새로 만들기"→ViewListGrid)으로 동작(서베이 §5) — 신규 컴포넌트 발명은 §10 게이트 4 위반. → W5-4 컴포지션 가이드 문서(W7 MIGRATION 전용 절이 흡수).
2. **ManyToOne 합성 필터 자동주입 미이식** [발명금지]: 구 getFilterableFields의 `<field>.name`/`<field>.user.name` 합성 필터 자동주입(8그룹 map "가장 복잡·의외" 지목)은 스펙 CAP-20("withFilter 파생")이 명세하지 않음 → 이식 안 함. 소비자가 해당 필드에 withFilter 명시 선언. 필요 판명 시 스펙 개정 선행.
3. **onFetchListData = onAfterListFetch로 이미 흡수**(§4·W2-6 기출하) → W5 태스크 아님.
4. **/presets 감사필드 헬퍼 구성**(§12 Open)은 list-track CAP(18/19/20) 밖 — /presets 헬퍼 태스크(§9 `withCreatedAndUpdatedAtFields` 이관)로 파킹, GJCU 관례 확인 입력. W5 리스트 트랙 비차단.

**서베이 발견(착수 전 반영 필수)**: 현 리스트 컬럼 opt-in은 **ManyToOneField 한정** `showInList`/`useListField()`(`many-to-one-field.ts:45,63-67`)뿐이고, "리스트 표시 필드 판정"이 **`ViewListGrid.tsx:75-98`·`xref-prefer-mapping-renderer.tsx:204-214` 두 곳에 duck-typed 중복**. W5-1이 withList로 일반화하며 이 중복을 공유 파생으로 제거(sample/consumer는 `columns` prop로만 컬럼 표현·useListField 미사용 — 서베이 §5). list-store fetch 파이프(onBefore/AfterListFetch·adapter.list·SearchForm.addAndFilter)는 W2-6 기출하 → W5-3 재사용, 변경 없음.

| id | 태스크 | 파일(HEAD 9209f8f) | before → after | 증명 | Do-NOT |
|---|---|---|---|---|---|
| W5-1 | 필드 list/filter 선언 substrate — **순수 additive** (§5.1) | 신규 `packages/schema-core/src/field/list-config.ts`(타입) · `field/form-field.ts`(빌더 :163-245 인접·저장 필드는 클래스 상단 :40-76 인접) · `schema-core/src/index.ts`(배럴 +2) · `__tests__` | 신설 타입 `FieldListConfig {order?, label?, align?, width?, sortable?}`·`FieldFilterConfig {operator?, order?, label?}`(§5.1 그대로·모든 프로퍼티 `p?: T \| undefined` 형 L4) · FormField `withList(config?: FieldListConfig \| false): this`+`getListConfig(): FieldListConfig \| false \| undefined`·`withFilter(config?: FieldFilterConfig \| false): this`+`getFilterConfig()` (in-place mutate+return this, §5.2) · **ManyToOne 미변경**(showInList/useListField 그대로 — 통합은 W5-2, 소비처 재작성과 동반; W5-1은 아무도 안 읽는 순수 신설이라 무파손) | unit: with*/get* round-trip · `false`=명시제외 vs undefined=미선언 구분 · 계수 /schema=184(≤190) | 마법 폴백·파생 정렬 금지(W5-2) · store 런타임 override 아님(선언-타임 인스턴스 필드 §5.2) · `FieldMetaOverride`에 list 키 추가 금지(별도 채널) · **ManyToOneField·ViewListGrid·소비처 건드리지 말 것**(W5-2 소관) · exactOptionalPropertyTypes 조건 spread 금지(전역 Do-NOT ③) |
| W5-2 | ViewListGrid 컬럼 파생 교체 + list-cell 레지스트리 + **ManyToOne showInList→withList 통합** (§7 CAP-19) | `packages/react/src/components/ViewListGrid.tsx:75-134`(deriveDefaultColumnNames/resolveColumns 교체) · 신규 `react/src/registry/list-cell-renderer-registry.tsx`(`field-renderer-registry.tsx:28-38` 패턴 복제) · `registry/xref-prefer-mapping-renderer.tsx:204-214`(중복→공유 파생·getListConfig 사용) · `packages/schema-core/src/field/many-to-one-field.ts:45,63-67`(useListField()→`this.withList()`·showInList 필드 제거) · `react/src/index.ts`(registerListCellRenderer +1) · apps/sample 리스트 페이지(필드 withList 이행) · `e2e/` | 컬럼 파생=getListConfig() truthy 필드 수집·정렬(config.order ?? field.order)·label/align/width 반영 · **마법 폴백 폐기**(withList 0 → 빈 컬럼+`console.warn`, §5.1 "첫 Listable 자동채택" 폐기) · `columns` prop escape hatch 유지 · sortable 헤더→`store.setSort` · 셀=`getListCellRenderer(type)` 조회(폴백 getDisplayValue/String, 현 bare `String()` :124/:132 대체) · showInList 두 duck-typed 소비처를 getListConfig() 공유 파생으로 일원화 | unit: 파생(order/label/align/width·false 제외·0→빈+warn)·useListField()⟹getListConfig() truthy · E2E: sample 폼 withList 선언→리스트 컬럼 렌더(College/Subject 재사용) | 첫-4-비숨김 폴백 유지 금지(§5.1 폐기) · 합성 컬럼 자동주입 금지 · `ViewEntityForm.tsx` 무관(별 hot-file) |
| W5-3 | 고급검색 패널 + filter 레지스트리 (§7 CAP-20) | `ViewListGrid.tsx`(고급검색 패널 내장 — W5-2 뒤 순차, 동일 파일) · 신규 `registry/filter-renderer-registry.tsx`(`field-renderer-registry.tsx:28-38` 패턴 복제) · `react/src/index.ts`(registerFilterRenderer +1) · list-store 재사용(`list-store.ts:71-143` fetch·`SearchForm.addAndFilter` 무변경) · `e2e/` | getFilterConfig() 필드 수집·정렬 → 필터 입력(`getFilterRenderer(type)` 폴백 기본) → `SearchForm.addAndFilter`(op=FieldFilterConfig.operator) → `setSearchForm`+`fetch`(W2-6 파이프 재사용) · 계수 root≈51 | unit: filter 파생(withFilter 필드·정렬) · E2E: 고급검색으로 sample 리스트 행 필터(College name) | **ManyToOne 합성 필터 자동주입 금지**(결정 2·스펙 미명세) · 별도 export 컴포넌트 신설 금지(ViewListGrid 내장 §7) · list-store fetch 계약 변경 금지 |
| W5-4 | 페이지 컴포지션 가이드(문서) + wave-end CAP/계수 대조 | 신규 `documents/plans/list-page-composition-guide.md` · apps/sample 정준 컴포지션 예시 | 결정 1대로 **컴포넌트 아님** — 호스트 페이지 셸 컴포지션(main/헤더/새로만들기/ViewListGrid) 문서화(W7 MIGRATION 전용 절이 흡수) · CAP-18/19/20 대조 · 계수 PASS(schema 184·root ~51·EntityForm 47 무변경) | Wrapper-급 컴포넌트 export 금지 · 스펙 §7 밖 표면 발명 금지 |

**W5 종료 게이트**: full gate + E2E(16→목표 +2: 컬럼 파생·고급검색) + CAP-18/19/20 대조 + 계수 리포트(/schema 184·root ~51·EntityForm 47 무변경) 커밋. **hot-file**: entity-form.ts/form-store.ts/ViewEntityForm.tsx 미관통(W5=form-field·ViewListGrid·신규 레지스트리 중심); `ViewListGrid.tsx`는 **W5-2→W5-3 순차**(동일 파일). **§10-A**: /schema W5 행 FieldListConfig·FieldFilterConfig(+2→184, 기재됨) · root 신규 registerListCellRenderer·registerFilterRenderer(+2→~51) · EntityForm +0. 임계값 근접 아님 → 재산정 불요, wave-end 실측 재검증.

## W6 — data-transfer (entry-brief pass ✅ 2026-07-12 · CAP-16·17)

> **entry 근거**: 스펙 §3.5(withDataTransfer/getDataTransfer + W6-entry 타입 확정 DataFieldSpec/DataTransferSpec)·§2(`/excel` subpath = DataExporter/Importer 런타임, xlsx optional peer)·§7(ViewListGrid `toolbar` render-prop = 호스트 컴포지션 seam)·§8 CAP-16/17·§9(withDataTransferConfig→withDataTransfer codemod)·§10-A(W6 /schema +2→186). 현 코드 서베이(2026-07-12 opus) — file:line은 HEAD `848c86f` 기준(착수 시 재확인). **공개 표면은 작다**(EntityForm +2·/schema 타입 +2); 구현은 구 `src/listgrid/transfer/` **3443 LOC** 서브시스템의 *코어 경로* 이식 + 신규 `/excel` 패키지가 phase급 무게. 스펙이 구 rich `withDataTransferConfig`(urls/mode/sampleData/maxCount/description 등)를 **최소 표면**(export.fields/fileName·import.fields)으로 의도적 축소 → W6 = 선언 표면(schema) + /excel 런타임 코어 + 툴바 컴포지션 + sample E2E.

**wave-entry 결정(스펙 인용 or 비크리티컬 모델 판정 — §10 게이트 4 준수)**:

1. **DataFieldSpec/DataTransferSpec = 선언 전용 schema 타입** [스펙 §3.5 개정으로 확정 2026-07-12]: 구 `DataField`(getValueOnExport/Import 런타임 메서드 보유)에서 **런타임 제거**한 선언형 `{name, label?, type?}`. 값 변환 스위치는 /excel 런타임이 `type`로 파생(L6 schema React/런타임 0). getDataTransfer()=**동기**(구 Promise는 isRequired await 탓, 신 spec은 required 미보유). 근거: L6·§3.5·§10-A(DataFieldSpec+반환형=/schema +2).
2. **:448 버그 = FIX(구조적)** [스펙 §3.5 명세]: import 폴백이 import.fields를 검사하도록 export/import **대칭 헬퍼 공유**로 복붙 버그를 표현 불가능하게. verbatim 이식 아님 — 스펙이 fix 결정(L8 구결함 재현 금지). 증명: export.fields 세팅+import.fields 빈 폼에서 두 getter 호출 시 import이 자기 fields로 폴백함을 실측.
3. **/excel = 신규 내부 패키지 `@listgrid/excel` → subpath `/excel`** [스펙 §2]: DataExporter/DataImporter/registry(configureDataTransfer/getDataTransfer DI)/registerExcelDataTransfer 이식. **xlsx-js-style+file-saver = optional peer**(이슈 #7 3분류 leaf 선례 — 코어 배럴 정적 import 0). packages/react 배럴 **무증가**(툴바 seam 재사용) → root +0.
4. **ViewListGrid `toolbar` seam 재사용(신규 prop 금지)** [스펙 §7·C7·W5-4 컴포지션 가이드 선례]: 호스트가 `toolbar?:(ctx:{checkedIds})=>ReactNode`(:82→render :437)로 Export/Import 버튼+`DataTransferModals`를 `getDataTransfer()` 먹여 렌더. packages/react는 data-transfer 빌트인 **무제공**(구 엔진 opt-in DI 패리티).
5. **복합 타입 값변환 제외+warn(모델 판정·runtime 기본)** [W5 결정2 발명금지 선례]: 신 FieldType 초집합(xref*/addressField/contentAsset/subCollection/mappedJoin 등 ~40 vs 구 ~28)에서 평면 셀 부재 타입은 auto-derive 제외+`console.warn`(구 identity 폴백=손실). 스칼라/옵션/날짜/불리언은 구 switch 이식. **정확 제외 목록은 W6-2서 확정·§Needs Review로 소비자 검증**.
6. **import 백엔드 seam = 호스트 공급(어댑터 표면 무증가)** [C7·§2 어댑터 자기 endpoint 소유 — Handoff Open Q 해소]: 구 `import.url = url+getEndpoint('excelUpload')`를 신 어댑터 메서드로 승격하지 **않고**, DataImporter가 호스트 공급 `uploadUrl`/`onUpload(rows)` prop 수신(C7 호스트가 통합 소유). sample E2E는 sample `/api/*/excel-upload` 라우트를 호스트로 배선. GJCU 관례가 어댑터 메서드를 요구하면 후속 스펙 개정(비차단).
7. **미이관 문서화 이연(charter C6 코어 밖)** [규율 4 고정목록]: officecrypto 비밀번호 export·excelDownloadHistory 로깅·DataImportSample(264)·DataImportResultView(232)·DynamicDataImporter(32)·DataExportService 이력(313) — 소비자 요구 시 /excel 확장(§9 MIGRATION 기재). 코어=export(클라 xlsx 생성)+import(파싱→POST).

**서베이 발견(착수 전 반영 필수)**: `withDataTransfer`는 **미존재**(구 `withDataTransferConfig` `src/listgrid/config/form/EntityFormActions.tsx:373`·:448 버그 확인). 신 엔진 `packages/*`는 data-transfer/excel **전무**(grep 0)·`@listgrid/react` package.json에 /excel subpath·xlsx/file-saver peer **없음**(root package.json의 /excel+peer는 구 0.3.26 단일패키지 표면). 구 서브시스템=`src/listgrid/transfer/` 3443 LOC + 헤더/훅 배선 ~1600 LOC. **신 엔진 data-transfer 소비자 0**(sample 리스트페이지 미선언·sample 백엔드 excel 엔드포인트 0) → "작동" 증명 위해 **sample 소비자+E2E 신설 필수**(export 하드게이트·import는 sample 엔드포인트 배선). `ViewListGrid.tsx:82` toolbar render-prop = 컴포지션 seam(render :437).

| id | 태스크 | 파일(HEAD 848c86f) | before → after | 증명 | Do-NOT |
|---|---|---|---|---|---|
| W6-1 | schema 선언 표면 — withDataTransfer/getDataTransfer + DataFieldSpec/DataTransferSpec + :448 구조적 fix (§3.5 CAP-16) | 신규 `packages/schema-core/src/data-transfer.ts`(타입 2) · `packages/schema-core/src/entity-form.ts`(멤버 2 + clone 전파 — withTitle/neverDelete/submitTransform 패턴 :134-138,270-280,326-345 인접) · `schema-core/src/index.ts`(배럴 +2) · `__tests__` | withDataTransfer(config): this 저장 + getDataTransfer(): DataTransferSpec\|undefined **동기 파생**(fields 미지정→this.getFields() 선언순 스냅샷·명시=verbatim) · export/import **대칭 헬퍼 공유**(import 폴백=import.fields, :448 fix) · **복합타입 제외 원칙**(정확목록=W6-2) | unit: with/get round-trip · **:448 회귀**(export.fields 세팅+import.fields 빈→import이 자기 fields로 폴백, export 아님) · auto-derive 선언순 · clone 전파 · 계수 /schema=186·EntityForm 49(≤190/55) | 런타임/React 금지(L6·순수 schema) · 구 rich config(urls/mode/sampleData/maxCount/description) 부활 금지(§3.5 최소표면) · getValueOnExport/Import 값변환 여기 금지(/excel) · isRequired async 파생 금지(동기 유지) · exactOptional 조건 spread(전역 Do-NOT ③) |
| W6-2 | `/excel` 런타임 패키지 — DataExporter/DataImporter/registry/registerExcelDataTransfer (§2 CAP-17) | 신규 `packages/excel/`(`@listgrid/excel`·package.json exports `.`·xlsx-js-style^1.2/file-saver^2 **optional peer**) · DataExporter(구 `transfer/DataExporter.tsx`201+`Provider/ExcelProvider.ts`251 코어=aoa_to_sheet/book_new+FileSaver) · DataImporter(구 `transfer/DataImporter.tsx`504 코어=파싱+createFieldMap+per-row 값변환+POST) · registry(구 `transfer/registry.ts`38 configureDataTransfer/getDataTransfer) · 값변환 스위치(구 `transfer/Type.ts` DataField.getValueOnExport/Import — 스칼라/옵션/날짜/불리언, **초집합 재감사**) · `__tests__` | 코어 export(필드→xlsx blob 다운로드)·import(xlsx→DataRowSet POST) · 값 round-trip(select label↔value `\|\|\|` 구분·date/datetime range·boolean 예/아니오·html/markdown strip) · **복합타입 제외+warn 목록 확정→§Needs Review** | unit: export 필드→시트 셀·import 파싱→행·값변환 각 타입 1 · 초집합 신타입(contentAsset/subCollection/mappedJoin) 제외+warn | 코어 배럴/schema-core 정적 xlsx import 금지(optional peer·/excel 전용) · officecrypto/password·downloadHistory·Sample템플릿·ResultView **이식 금지**(결정7 이연) · 어댑터 표면 증가 금지(결정6 호스트 공급) · **역방향 의존 금지: schema-core가 /excel import 금지**(L6·ADR-0003 순수성 — /excel은 소비 계층이라 schema-core 타입 `DataFieldSpec` import 허용) |
| W6-3 | 툴바 컴포지션 + sample 소비자 + E2E (§7 CAP-16/17 UI) | `packages/react/src/components/ViewListGrid.tsx`(**미변경** — toolbar :82 seam 재사용 확인만) · `apps/sample`(리스트페이지 1[College] withDataTransfer 선언 + toolbar로 Export/Import 버튼+DataTransferModals 배선 + registerExcelDataTransfer 부트스트랩) · sample 백엔드 `app/api/college/excel-upload/route.ts`(import POST 수신·호스트 소유 C7) · `e2e/` | 호스트 컴포지션: getDataTransfer()→toolbar 버튼→/excel 모달 · **E2E export(하드): College 리스트 Export→xlsx 다운로드**(클라 100%·백엔드 불요) · E2E import: xlsx 업로드→sample 엔드포인트 POST→행 반영 | E2E: export 다운로드 파일 존재·시트 헤더=필드 label · import: 업로드 행이 목록 재조회에 출현 | ViewListGrid 신규 prop 금지(toolbar seam만·§7/C7) · packages/react 배럴에 data-transfer export 금지(root +0) · 신규 페이지-셸 컴포넌트 발명 금지(W5-4 가이드=호스트 컴포지션) |
| W6-4 | wave-end — CAP/계수/MIGRATION 대조 | (문서) · `documents/plans/entityform-public-api-spec.md`(§9 codemod 행·MIGRATION 이연목록) · `scripts/count-public-surface.mjs` 리포트 | CAP-16(withDataTransfer :448 fix)·CAP-17(/excel xlsx optional) 전건 착지 대조 · 계수 실측(/schema 186·EntityForm 49·root 57 무변경·/excel=미계수 신subpath) | full gate + E2E(28→목표 +2 export/import) green · CAP 빈행 0 · §10-A W6 행 실측 일치 | 스펙 §7 밖 표면 발명 금지 · 미소화 CAP 은닉 금지(명시 이월) |

**W6-2 착수 노트 (scaffold + value-transform tiers 확정 2026-07-12 opus · 서베이 근거)**:
- **scaffold(npm workspaces)**: 신규 `packages/excel/`(`@listgrid/excel`·0.0.0·private·type:module·`exports {".":"./src/index.ts"}`·`@listgrid/react` shape 미러) · deps `@listgrid/schema-core`+`@listgrid/react`+`@listgrid/state` · **peers `xlsx-js-style ^1.2`+`file-saver ^2.0.5`+`peerDependenciesMeta{optional:true}`**(leaf 최초 — root 0.3.x `package.json:247-303` 패턴 미러) · `tsconfig.json`=`packages/state/tsconfig.json` 클론(composite·references [schema-core,react,state]) · **root `tsconfig.json:39-48` references에 `{path:"./packages/excel"}` 1줄 추가**(=tsc -b 픽업). tsup(published)은 src/ 기반이라 W6는 무관(W7 패키징서 published exports 매핑).
- **값변환 tiers(결정5 확정·구 `transfer/Type.ts:531-620` 이식)**: **TIER1 transform**(구 switch verbatim)=`select`(option value↔label)·`multiselect`(`|||` 구분 다중)·`date`/`datetime`(range `~` 포맷)·`boolean`(예/아니오)·`html`/`markdown`(plain-text strip). **TIER2 passthrough**(`String(v)`·null→'')=그 외 전 primitive(text/number/email/phone/time/month/year/tag/checkbox/textarea/password/birthday/telephoneNumber/color/colorPreset/link/manyToOne/xref*/address 등 — **구엔진 default 패리티**, 무손실 회귀 없음). **TIER3 export 제외+`console.warn`**(평면셀 무의미 구조타입 7종)=`subCollection`·`contentAsset`·`multipleAsset`·`file`·`image`·`inlineMap`·`mappedJoin`. **W6-2b 확정**: TIER3 제외는 `getDataTransfer()` 반환 필드에 **uniform 적용**(schema는 명시/파생 discriminator 미보유·순수 선언 → /excel이 구분 없이 필터). flat cell 무의미(String=garbage)하므로 명시여도 제외가 정답 — 복합값은 소비자가 스칼라 평탄화. **§Needs Review**: TIER 경계는 실 GJCU 데이터 미검증(M2O/xref/address 평면행 값이 실제 export 가능한지 소비자 확인 — 필요 시 TIER3 편입).
- **2-step 위임 분해**(신규 패키지=공유 디렉토리·병렬 편집 위험 → 순차): **W6-2a**(foundation) scaffold+root ref+registry DI(`configureDataTransfer`/`getDataTransfer`·구 `transfer/registry.ts` 이식)+값변환 모듈(tiers)+`registerExcelDataTransfer`+배럴+unit(값변환 각 타입); tsc -b 픽업 검증 후 커밋. **W6-2b**(UI/IO) DataExporter(export core=구 `Provider/ExcelProvider.ts:71-219` aoa_to_sheet/header포맷/XLSX.write/FileSaver·**password/officecrypto·downloadHistory 제외**)+DataImporter(import core=구 `DataImporter.tsx:113-387` XLSX.read/sheet_to_json/`[name]` 헤더매칭/per-cell getValueOnImport/POST·**preview/Sample/Result UI 제외**)+thin 모달 컴포넌트+unit; 2a 검증 후 착수.

**W6 종료 게이트**: full gate + E2E(28→목표 +2: export 다운로드·import 업로드) + CAP-16/17 대조 + 계수 리포트(/schema 186·EntityForm 49·root 57 무변경·`/excel`=신규 subpath 미계수) 커밋. **hot-file**: `entity-form.ts`는 W6-1이 관통(withDataTransfer 멤버+clone) — W6 내 schema 태스크는 W6-1 단일이라 직렬 충돌 없음; form-store.ts/ViewEntityForm.tsx 미관통. **§10-A**: /schema W6 행 DataFieldSpec·DataTransferSpec(+2→186, 기재됨) · EntityForm +2(withDataTransfer/getDataTransfer — **실측 47→49**; §10-A 최종 53은 §3.2 질의 완비 시점 상한-근거로 49는 그 궤적 중간점) · root +0(toolbar 재사용, 실측 57 유지) · **`/excel` 신규 subpath는 root/schema/EntityForm 3-예산 밖**(구 /ui-default 등과 동일·미계수). **마진 주의**(재산정 불요 근거): /schema 186은 §10-A +4 마진 내(W7 schema 타입 +0 → 최종 186 확정)·EntityForm 최종 53은 +2 마진 내 — 임계값(55/120/190)이 **이미 최종 설계 기준으로 산정**(§10-A 재산정 2026-07-11)되어 W6 추가가 마진을 소진하지 않음. wave-end 실측(count-public-surface.mjs) 재검증 필수.

## 완료 정의 (전 wave)

스펙 §10 게이트 1~7 전부 + §8 CAP-01~29 전 행 소화/명시 이월 + 구 결함 원장 §1~9 봉인 증거표. GA 게이트(헌장 대조표)는 W7 뒤 별도 pass.
**wave 미할당 CAP 계정**: CAP-01·CAP-15(기출하/현행 유지 — 매 wave 회귀 게이트) · CAP-28(GA 대조표 pass) · CAP-29(descope — 대상 없음). 이 4행은 대조 시 "이월 아님·계정 완료"로 처리한다.
