name: CI

on:
  push:
    branches: [main, v0.4]
  pull_request:
    branches: [main, v0.4]

jobs:
  quality:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: [20, 22]
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install
        run: npm ci --legacy-peer-deps

      - name: Type check
        run: npm run type-check

      - name: Lint
        run: npm run lint

      - name: Format check
        run: npm run format:check

      - name: Test + coverage (enforces vitest.config.ts thresholds)
        run: npm run test:coverage

      - name: Build
        run: npm run build

      - name: Verify build output
        run: |
          test -f dist/index.js
          test -f dist/index.d.ts
          test -f dist/styles.css
          test -f dist/styles/tokens.css
          test -f dist/styles/primitives.css
          test -f dist/styles/layouts.css
          test -f dist/styles/components.css
          test -f dist/styles/base.css
