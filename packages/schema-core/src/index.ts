// @listgrid/schema-core — EntityForm declarations, field metadata, validation, SearchForm, PermissionPolicy — React-free (lint-enforced)
//
// Re-foundation scaffold (P1). Transplant target for P4/P5 — verified logic
// from the repo-root src/ is moved here module-by-module under the
// characterization-test oracle (ADR-0008). Empty until then.

// P1-5 workspace-wiring marker — apps/sample imports this to prove the
// @listgrid/* workspace path resolves (npm workspaces symlink) and
// live-reloads during `next dev`. Bump when this package starts shipping
// real transplanted modules.
export const SCHEMA_CORE_VERSION = '0.0.0';
