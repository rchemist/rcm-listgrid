// @listgrid/next — Next.js adapter (ADR-0003 adapters layer)
//
// Re-foundation scaffold (P1). Transplant target for P4/P5 — verified logic
// from the repo-root src/ is moved here module-by-module under the
// characterization-test oracle (ADR-0008).
export { NextRouterProvider } from './NextRouterProvider';
export type { NextRouterProviderProps } from './NextRouterProvider';
